# 依賴模組 (確保忽略所有 node_modules)
node_modules/
frontend/node_modules/ # 確保忽略前端的依賴
backend/node_modules/  # 確保忽略後端的依賴

# 環境變數設定檔 (確保忽略所有 .env 檔案)
.env
frontend/.env          # 確保忽略前端的 .env
frontend/.env.productuin
backend/.env           # 確保忽略後端的 .env

# 日誌檔案
*.log
npm-debug.log*

# 作業系統和編輯器相關檔案 (如果使用 macOS 或 VS Code)
.DS_Store
.vscode/

# 編譯或打包輸出目錄
frontend/dist/         # 前端 Vue 專案打包後的目錄
backend/build/         # 後端 TypeScript 等編譯後的目錄 (如果有的話)